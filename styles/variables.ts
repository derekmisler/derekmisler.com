const orange = '#D94700'
const red = '#BB1F28'
const green = '#2DC22D'
const yellow = '#FFCF9A'
const blue = '#253a57'

const bodyBg = '#ffffff'
const textColor = '#2A2C2B'

const linkColor = orange
const linkColorHover = red

const sans = '"Lato", "Helvetica Neue", Helvetica, sans-serif'
const serif = '"Playfair Display", Georgia, serif'

const fontSizeBase = 22
const fontSizeLarge = Math.ceil(fontSizeBase * 2)
const fontSizeSmall = Math.ceil(fontSizeBase * 0.85)

const lineHeightBase = 1.5
const scale = 1.414

const navHeight = '40px'

export default {
  bodyBg,
  textColor,
  linkColor,
  linkColorHover,
  sans,
  serif,
  fontSizeBase,
  fontSizeLarge,
  fontSizeSmall,
  lineHeightBase,
  scale,
  navHeight
}
