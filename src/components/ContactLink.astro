---
import SocialIcon from '@/components/SocialIcon.astro';

type SocialIconName = 'github' | 'linkedin' | 'gmail' | 'twitter' | 'youtube' | 'instagram' | 'trophy';

interface Props {
  href: string;
  label: string;
  altLabel?: string;
  download?: string;
  icon?: SocialIconName;
  /** nav: .nes-btn button style (header nav); detail: h4 + link (contact section) */
  variant?: 'nav' | 'detail';
}

const { href, label, altLabel, download, icon, variant = 'nav' } = Astro.props;
---

{
  variant === 'nav' ? (
    <a class="nes-btn" href={href} download={download} target="_blank" rel="noopener noreferrer">
      {label}
    </a>
  ) : (
    <div class="contact-detail">
      <h3 class="contact-label">
        {icon && (
          <span class="contact-icon" aria-hidden="true">
            <SocialIcon name={icon} href={href} label={label} decorative />
          </span>
        )}
        {label}
      </h3>
      <p>
        <a href={href} download={download} target="_blank" rel="noopener noreferrer">
          {altLabel ?? label}
        </a>
      </p>
    </div>
  )
}

<style>
  .contact-label {
    display: flex;
    align-items: center;
    gap: var(--space-xs);
  }
  /* Icon is presentational here; pointer-events still fine since it's aria-hidden */
  .contact-icon {
    display: inline-flex;
  }
</style>
